describe('projectDataService', function() {

  beforeEach(module('CountMe'));

  it('should ...', inject(function(projectDataService) {

    //expect(projectDataService.doSomething()).toEqual('something');

  }));

});
